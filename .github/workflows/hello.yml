name: Hello World Test

on:
  push:
    branches:
      - prod  # Trigger only on commits to the 'prod' branch
  workflow_dispatch:  # Allow manual trigger

jobs:
  test-runner:
    runs-on: [self-hosted, dt-dev-app-vm2]  # Exact runner label
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print Hello World
        run: echo "✅ Hello from dt-dev-app-vm2!"

